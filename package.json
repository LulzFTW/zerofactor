{
  "name": "zerofactor",
  "version": "1.0.0",
  "description": "Circumvents Duo two-factor authentication.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify zerofactor.js -o addon/zerofactor.js"
  },
  "author": "lulzftw",
  "license": "GPL-3.0",
  "devDependencies": {
    "browserify": "^14.4.0"
  },
  "dependencies": {
    "notp": "^2.0.3"
  }
}
