{
  "name": "zerofactor",
  "version": "1.0.0",
  "description": "Circumvents Duo two-factor authentication.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack && web-ext build -s extension --overwrite-dest"
  },
  "author": "lulzftw",
  "license": "GPL-3.0",
  "dependencies": {
    "notp": "^2.0.3"
  },
  "devDependencies": {
    "webpack": "^3.6.0"
  }
}
